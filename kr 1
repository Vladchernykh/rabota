#include <iostream>
#include <fstream>
#include <math.h>
#include <stdio.h>
using namespace std;

int main()
{
    int a,b = 0;
    cin >> a; // введенное число
    for (int i = a+1; i <= -1; i++)
    {
       if (-i%2 == 1) 
       b = i + b;
    }
    cout << b;

    return 0;
}
