#include <iostream>
#include <math.h>

using namespace std;

int main()
{
	const int N = 10;
	int M[N];
	double l = 0;
	for (int i = 0; i < N; i++)
		cin >> M[i]; // ввод чисел в массив

	for (int i = 0; i < N; i++)
		l += M[i];
	cout << "Мат.ожидание = " << (l / N);
	return 0;
}
